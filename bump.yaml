bump.yaml:
  ^(.*)(0.955)(.*)$: replace
pythonizer:
  '^(# )(0.955)(  )(\d\d\d\d\/\d\d\/\d\d)(  SNOOPYJC)': append
  ^(.*\$VERSION=')(0.955)(';): replace
setup.py:
  ^(.*version=')(0.955)(',): replace
user_guide.html:
  ^(.*Version )(0.955)( \()([A-Z][a-z][a-z] \d*, \d\d\d\d)(\).*)$: replace
HISTORY.rst:
  ^(0.955)( \()(\d\d\d\d-\d\d-\d\d)(\))$: history
